# Secrets — never commit
.env
.env.local
.env.*.local
*.key

# Python
__pycache__/
*.py[cod]
.venv/
venv/
.pytest_cache/
.ruff_cache/
*.egg-info/

# Node / Next
node_modules/
.next/
out/
npm-debug.log*
yarn-error.log*

# Build artefacts — regenerated by `python -m scripts.seed` on every deploy.
# The CSVs in backend/data/raw/ are the single source of truth.
backend/data/build/

# OS / editor
.DS_Store
.vscode/
.idea/
